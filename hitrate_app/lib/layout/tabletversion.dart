import 'package:flutter/material.dart';

class Tabletversion extends StatelessWidget {
  const Tabletversion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
